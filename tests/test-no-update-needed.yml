name: Test - No Update Needed (SDK already current)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Get latest .NET 8.0 SDK version
        id: get-sdk
        run: |
          LATEST=$(curl -s https://raw.githubusercontent.com/dotnet/core/main/release-notes/8.0/releases.json | python3 -c "import sys,json; d=json.load(sys.stdin); print(d['latest-sdk'])")
          echo "Latest 8.0 SDK: $LATEST"
          echo "version=$LATEST" >> "$GITHUB_OUTPUT"

      - name: Create global.json with latest SDK version
        run: |
          mkdir -p "$GITHUB_WORKSPACE/test-workspace-noupdate"
          cat > "$GITHUB_WORKSPACE/test-workspace-noupdate/global.json" <<EOF
          {
            "sdk": {
              "version": "${{ steps.get-sdk.outputs.version }}"
            }
          }
          EOF
          echo "Created global.json with version ${{ steps.get-sdk.outputs.version }}"
          cat "$GITHUB_WORKSPACE/test-workspace-noupdate/global.json"

      - id: action
        uses: .
        with:
          repo-token: ${{ github.token }}
          global-json-file: ${{ github.workspace }}/test-workspace-noupdate/global.json
          channel: '8.0'
          generate-step-summary: 'false'

      - name: Assert sdk-version output is non-empty
        env:
          SDK_VERSION: ${{ steps.action.outputs.sdk-version }}
          SDK_UPDATED: ${{ steps.action.outputs.sdk-updated }}
        run: |
          echo "sdk-version: $SDK_VERSION"
          echo "sdk-updated: $SDK_UPDATED"
          if [ -z "$SDK_VERSION" ]; then
            echo "FAIL: sdk-version output is empty"
            exit 1
          fi
          echo "PASS: sdk-version is '$SDK_VERSION'"

      - name: Assert sdk-updated is false (no update needed)
        env:
          SDK_UPDATED: ${{ steps.action.outputs.sdk-updated }}
        run: |
          echo "sdk-updated: $SDK_UPDATED"
          if [ "$SDK_UPDATED" != "false" ]; then
            echo "FAIL: sdk-updated should be 'false' when using the latest version, got '$SDK_UPDATED'"
            exit 1
          fi
          echo "PASS: sdk-updated is 'false' as expected"
